# This example covers all available features of Conjoint language.

import {printLine} from "io";

let a:Number = 1; # Numeric variable
let b:Character = 'a'; # Character variable
let c:String = "1"; # String variable
let d:String? = null; # Strint variable
let e:Boolean = true; # Boolean variable

printLine(d); # Write line to output